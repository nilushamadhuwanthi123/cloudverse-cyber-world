# `styles/`

Design tokens and global styles: colour, typography, spacing, motion.

Built in STEP 2 (Visual Foundation). Districts consume tokens from here
rather than hard-coding hex values, so a single change restyles the
world instead of the same colour being buried in nine files.
