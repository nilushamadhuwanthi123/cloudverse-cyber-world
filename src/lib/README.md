# `lib/`

Small generic helpers with no CLOUDVERSE knowledge — formatting, timing,
and the anime.js wrappers that make sure every animation is cleaned up on
unmount and respects `prefers-reduced-motion`.

If a helper mentions threats, missions or health, it belongs in `game/`.
