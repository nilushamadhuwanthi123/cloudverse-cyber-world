# `app/`

Application shell: providers, layout, and the top-level composition that
decides which district is on screen.

Nothing routes by URL — CLOUDVERSE is a single-screen world, so "where
am I" is application state, not a URL. Keep that state here.
